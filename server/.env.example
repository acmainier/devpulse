# MySQL database name
DB_NAME=devpulse

# MySQL username
DB_USER=root

# MySQL password for the above user
DB_PASSWORD=your_password_here

# Host where MySQL is running (usually localhost for local dev)
DB_HOST=localhost

# Secret key used to sign JWTs — use a long random string, keep it private
JWT_SECRET=your_jwt_secret_here